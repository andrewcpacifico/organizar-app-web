machine:
    node:
        version: 6.10.1    
    timezone:
        America/Manaus

test:
  override:    
    - ng lint
    - ng test --single-run
  post:
    - ng build

dependencies:
  override:
    - npm i -g npm
    - npm i -g @angular/cli
    - npm install