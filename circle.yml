machine:
    node:
        version: 6.10.1    
    timezone:
        America/Manaus

test:
  override:    
    - npm run lint
    - npm test
  post:
    - npm run build

dependencies:
  override:
    - npm i -g npm
    - npm install