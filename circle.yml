machine:
    node:
        version: 6.10.1
    npm:
        version: 4.4.4
    timezone:
        America/Manaus

test:
  override:    
    - npm run lint
    - npm test
  post:
    - npm run build

dependencies:
  override:
    - npm install